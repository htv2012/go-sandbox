info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/movies
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "isbn": "199955",
        "title": "Once More"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
