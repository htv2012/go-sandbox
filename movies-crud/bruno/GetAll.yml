info:
  name: GetAll
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/movies
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
